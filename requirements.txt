# Core (required for `fixture` run mode and all extractor modules)
sqlglot>=25.0.0

# Required only for `live` run mode against a real SQL Server instance.
# Also requires a system ODBC driver (e.g. "ODBC Driver 18 for SQL Server").
pyodbc>=5.0.0

# Optional: only needed if AUTOVISTA_LLM_FALLBACK_ENABLED=true,
# AUTOVISTA_LLM_COMPAT_NOTES_ENABLED=true, or LAKEBRIDGE_LLM_COMPAT_NOTES_ENABLED=true
anthropic>=0.40.0

# Dev/test
pytest>=8.0.0

# --- Lakebridge Discovery (independent second Discovery engine) ---
# openpyxl is only needed if lakebridge_discovery/report_parser.py has to
# fall back to reading the Analyzer's .xlsx report (no --generate-json).
# The Lakebridge Analyzer itself is NOT a pip package this project imports --
# it's the separate `databricks labs lakebridge analyze` CLI, installed via
# the Databricks CLI. See README.md "Installing Lakebridge" for the full
# (non-pip) install path.
openpyxl>=3.1.0
